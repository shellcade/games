# shellcade-username -> GitHub login.
# INTERIM mapping: CI uses this to enforce that games/<shellcade-username>/...
# may only be changed by the linked GitHub account. Entries are added in an
# author's first PR and verified against the in-arcade link by a maintainer
# before merge. A public shellcade.com API will replace this file.

bcook = "BCook98"
alan = "astephensen"
